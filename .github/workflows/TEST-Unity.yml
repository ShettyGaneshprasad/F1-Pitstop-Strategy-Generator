name: Unit Testing - Unity

on:
  push:
    branches: [Production]
  pull_request:
    branches: [Production]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: changeDir
        run: cd 3_Implementation/
      - name: test
        run: make -C test
